# 첫인상과 군더더기 — 상태

> 배경과 완료 조건은 `README.md`, 단계와 검증 방법은 `plan.md`에 있습니다.
> 여기는 지금 어디까지 왔고 다음이 무엇인지만 다룹니다.

## 작업 위치

| 항목 | 값 |
| --- | --- |
| 작업 위치 | `workstream/007-first-impression` |
| 기준 브랜치 | `main` |
| 반영 대상 브랜치 | `main` |
| 원격 작업 흔적 | 남기지 않음 — 원격 작업 브랜치·PR·병합 커밋 없음 |

**이 값들은 하나씩 묻지 않고 정했습니다.** 사용자가 "로컬 브랜치에서 실행하고
커밋 알아서 진행"이라고 해서 `workstreams.md`의 「작업 위치를 정할 때」 질문
순서를 밟지 않았습니다. 006과 같은 관례를 씁니다.

**계획 커밋도 이 브랜치에 뒀습니다.** `workstreams.md`의 「시작할 때」 7번은
계획을 기준 브랜치에 커밋하라고 하지만, 사용자가 브랜치 안에서만 작업하라고
해서 `main`을 건드리지 않았습니다. push도 하지 않으므로 다른 세션이 이 계획을
보려면 이 브랜치를 봐야 합니다.

**받은 승인의 범위**

- 워크스트림 생성 — 받음
- 이 브랜치 안의 파일 변경과 커밋 — 받음, 건별로 다시 묻지 않음
- push — **받지 않음**
- 대작업 종료와 아카이브 — **받지 않음**

## 현재 단계

계획을 세웠습니다. `plan.md`의 1단계부터 진행합니다.

## 직전 완료

없습니다. 방금 연 대작업입니다.

## 다음 행동

`plan.md`의 1단계 — 0.1.x 절 안에 섞인 아카이브 취급 설명을 별도 질문으로
옮깁니다.

## 막힌 점과 열린 질문

- 0.1.x 감지를 지우면 그 구조를 쓰는 프로젝트가 「필수 문서가 없습니다」만
  받습니다. 무엇이 어디로 갔는지는 알려 주지 못합니다. 쓰는 사람이 없다고
  보고 지우는 것이라, 그 판단이 틀리면 되돌려야 합니다.
- 버전은 올리지 않습니다. 검사기 동작이 좁아지므로 다음 릴리즈는 `0.2.1`이
  아니라 `0.3.0`이 맞다고 보지만, 릴리즈는 따로 승인받고 합니다.
