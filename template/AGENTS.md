<!-- 채우기: 프로젝트 이름 -->
# 프로젝트 이름

<!-- 채우기: 이 프로젝트가 무엇을 하는지 두세 줄 -->

이 파일은 AI 코딩 에이전트가 이 저장소에서 지켜야 할 규칙입니다. Codex는 이
파일을 직접 읽고, Claude Code는 `CLAUDE.md` 맨 위의 `@AGENTS.md` import를 통해
읽습니다. 두 도구가 같은 원본을 보도록 프로젝트 규칙은 이 파일과 여기서 안내하는
문서에 둡니다. 공통 규칙을 `.claude/rules/`처럼 한 도구만 읽는 곳에 두지
않습니다. 같은 규칙의 상세 내용을 여러 문서에 독립된 원본으로 복제하지도
않습니다. 다만 항상 지켜야 할 핵심 제약과 작업 시점별 체크리스트는 필요한
위치에서 짧게 다시 확인할 수 있습니다.

**이 파일에 적힌 규칙은 항상 적용됩니다.** 긴 내용은 아래 파일에 두고, 해당
작업을 할 때 읽습니다.

새 세션에서 이 저장소의 작업을 시작할 때는 첫 작업 전에 `.agents/rules/` 아래의
규칙 문서를 모두 읽습니다. 파일이 나중에 추가될 수 있으므로 안내표에 적힌 파일만
보지 말고 폴더 전체를 확인합니다. 그 뒤에도 아래 표가 가리키는 시점에 해당 문서를
다시 읽습니다.

- `CLAUDE.md`는 `@AGENTS.md` 한 줄로 유지합니다.
- `AGENTS.md`는 가급적 200줄 미만으로 유지합니다. 길어지면 항상 필요한
  규칙과 문서 안내만 남기고, 상세 내용은 관련 문서로 옮깁니다.

| 언제 | 무엇을 |
| --- | --- |
| 다 됐다고 보고하기 전 | `.agents/rules/verification.md` |
| 답변을 쓸 때 | `.agents/rules/communication.md` |
| 지난 작업을 이어받거나 대작업을 시작·완료할 때 | `.agents/plans/workflow.md` |
| 계획 문서의 구조를 확인할 때 | `.agents/plans/README.md` |
| 무엇이 왜 정해졌는지 확인할 때 | 해당 대작업의 `workflow.md`와, 별도 근거가 있을 때 `decisions.md` |
| 목표와 전체 범위를 볼 때 | `.agents/plans/goal.md` |
| 향후 아이디어를 볼 때 | `.agents/plans/ideas.md` |
| 마친 대작업을 찾을 때 | `.agents/plans/history.md` |
<!-- 채우기: 이 프로젝트에만 있는 문서를 여기에 한 줄씩 더합니다.
     예) | 배포 절차를 다룰 때 | `docs/DEPLOY.md` | -->

`.agents/`에는 상황별 상세 규칙과 프로젝트 운영 기록을 둡니다.
`.agents/rules/`에는 검증과 답변 방식처럼 작업 시작 때 확인하고 정해진 시점에
다시 적용할 상세 규칙을 둡니다. 이 경로 자체는 Codex나 Claude Code가 자동으로
읽는 특별한 경로가 아니므로, 위의 시작 규칙과 안내표를 통해 읽도록 연결합니다.

`.agents/plans/`에는 목표, 현재 상태, 이력과 아이디어를 둡니다. 현재 상태는 진행
중인 대작업의 `workflow.md`에서 관리합니다. 대작업을 마치면 완료든 취소든
`history.md`에 짧게 기록하고 해당 폴더를 `.agents/archive/workstreams/`로 옮깁니다.

<!-- 고르기: `.agents/`를 커밋할지 정하고 아래 둘 중 하나만 남깁니다.
     커밋한다: 기록이 저장소에 함께 남아 다른 기기와 협업자에게 이어집니다.
     커밋하지 않는다: `.gitignore`에 넣습니다. 저장소가 공개일 때 선택하지만,
     기록을 백업할 다른 방법이 필요합니다. -->
`.agents/`는 `AGENTS.md`, `CLAUDE.md`와 함께 커밋합니다.

## 답변 언어

- 사용자가 다른 언어로 바꾸기 전까지 한국어로 답합니다.
<!-- 기본값입니다. 쓰는 언어에 맞게 바꿉니다. -->

## 프로젝트 설정

<!-- 채우기: 런타임, 언어, 패키지 관리자, 빌드·배포 방식처럼
     매번 다시 묻지 않아도 되도록 이미 정해진 것을 적습니다. -->
- 런타임과 언어:
- 패키지 관리자:
- 그 밖에 이미 정해진 것:


## 하지 않는 것

<!-- 채우기: 작업 중에 매번 걸리는 선을 짧게 적습니다. 여기가 비어 있으면
     범위가 계속 넓어집니다. 그렇게 정한 배경은 `.agents/plans/goal.md`에
     적고, 여기에는 지켜야 할 선만 남깁니다.
     예) 사용자 데이터를 외부로 보내지 않습니다.
     예) 자동 배포는 하지 않습니다. 배포는 사람이 실행합니다. -->
## 보안과 개인정보

- API 키, 토큰, 비밀번호, `.env` 파일과 그 밖의 자격증명을 커밋하지 않습니다.
- 개인정보도 마찬가지입니다. 로그, 테스트 출력, 생성 아티팩트에도 시크릿이나
  개인정보가 들어가지 않게 확인합니다.
- 스테이징 전에 실제 diff를 확인합니다. 시크릿이나 개인정보가 보이면 요청받은
  파일이라도 커밋하지 말고 멈춰서 알립니다.
<!-- 채우기: 저장소가 공개인지 비공개인지, 실제 값을 어디에 두는지
     (예: GitHub Secrets) 적어 두면 판단이 빨라집니다. -->

## 작업 전 승인

<!-- 기본값입니다. 팀 사정에 맞게 조정합니다. -->

- 파일을 읽고 상태를 확인하거나 질문에 답하는 일은 별도 승인이 필요 없습니다.
- 질문에 답하거나 아이디어를 비교하는 단계에서는 답만 하고, 사용자가 "시작해",
  "진행해"처럼 명확히 요청했을 때 구현을 시작합니다. 질문인지 진행 신호인지
  애매하면 질문으로 보고 되묻습니다.
- 작업이 시작된 뒤에는 합의한 범위 안의 개별 편집마다 다시 승인받지 않습니다.
- 새 의존성은 합의한 작업 중이라도 추가 전에 용도와 대안을 설명하고 승인을
  받습니다.
- AI가 자동으로 읽는 지시 파일을 만들거나 고치거나 지우기 전에는 바꿀 내용을
  제시하고 승인을 받습니다. 현재 파일이 없더라도 새로 만드는 경우를 포함하고,
  도구가 자동으로 만든 파일도 예외가 아닙니다. `AGENTS.md`, `CLAUDE.md`,
  `AGENTS.override.md`, `CLAUDE.local.md`, `.claude/rules/`, `.agents/`와
  사용자 전역 지시 파일이 해당합니다.

## Git과 커밋

<!-- 기본값입니다. 팀 사정에 맞게 조정합니다. -->

- 커밋과 `git push`는 각각 실행 전에 승인을 받습니다. PR 생성과 원격 저장소
  변경도 마찬가지입니다. 로컬 브랜치 생성은 승인이 필요 없습니다.
- 커밋 시점은 스스로 잡습니다. "커밋해"라는 말을 기다리지 말고, 작업이 한 덩어리
  끝나면 어떤 파일을 어떤 메시지로 커밋할지 먼저 제안합니다. 긴 작업에서도
  성격이 다른 변경이 섞이기 전에 끊어서 제안합니다.
- 성격이 다른 변경을 한 커밋에 섞지 않습니다. 각 커밋은 따로 이해하고 검증할
  수 있는 단위로 나눕니다. 의존성과 lockfile 변경은 그것을 실제로 쓰는 작업과
  함께 묶습니다.
<!-- 채우기: 커밋 메시지 언어와 형식을 정합니다. 기존 `git log`를 따르는 것이
     보통 맞습니다. 예) 한국어로 씁니다 / `type(scope): subject` 형식을 씁니다 -->
- 커밋 메시지 언어:
- 커밋 메시지 형식:
- 커밋 메시지에 `Co-Authored-By:` 트레일러를 기본적으로 붙이지 않습니다.
