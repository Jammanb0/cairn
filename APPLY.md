# 적용 절차

이 문서는 `cairn` 골격을 **이미 작업 중인 프로젝트**에 얹기 시작하는 안내입니다.
에이전트에게 읽혀도 되고, 사람이 직접 읽고 따라 해도 됩니다.

에이전트에게 시킬 때는 이렇게 말하면 됩니다.

> `.cairn/APPLY.md`를 읽고 이 프로젝트에 적용해줘. 기존 규칙과 기록은 보존하고,
> 지시 파일 변경안은 적용 전에 보여줘.

여기서는 조사하고, 물어보고, **세팅 워크스트림을 만드는 데까지**만 합니다. 실제
이관과 병합 절차는 그 워크스트림의 `workflow.md`에 있습니다. 같은 절차를 두 곳에
적지 않습니다.

**지시 파일은 새로 만드는 것도 승인을 받고 씁니다.** 무엇을 어떻게 쓸지 먼저
보여준 뒤에 씁니다.

---

## 0. 먼저 조사한다

무엇이든 쓰기 전에 아래를 확인하고 결과를 사람에게 알립니다.

### 기존 프로젝트인지 가른다

`.cairn/`과 `.git/`은 빼고 봅니다. `.cairn/`은 적용하려고 받아 온 복사본이고
`.git/`은 저장소 메타데이터라, 둘 다 프로젝트에 내용이 있는지 판단하는 근거가
되지 않습니다.

- 대상 루트에 실제 파일이 있는가 — 코드, 설정, 문서 무엇이든
- 지시 파일이 있는가 — `AGENTS.md`, `CLAUDE.md`, `.agents/`, `.cursorrules`,
  `.github/copilot-instructions.md`, `.claude/rules/`
- 진행 기록이 있는가 — `TODO.md`, `NOTES.md`, `ROADMAP.md`, `docs/` 아래 계획
  문서, 핸드오프 문서
- README에 목표나 범위 설명이 있는가
- 커밋이 하나라도 있는가

**하나라도 있으면 기존 프로젝트입니다.** 1번으로 갑니다.

하나도 없으면 옮길 것이 없으므로 세팅 워크스트림이 필요 없습니다. 골격만 바로
놓습니다.

```bash
npx --yes github:Jammanb0/cairn init <폴더>
```

이미 만들어 둔 빈 폴더 안이라면 `.cairn/template/`을 그대로 복사해도 같습니다.
그다음 `<!-- 채우기: -->` 자리를 채우면 끝입니다. 아래 절차는 밟지 않습니다.

### 기존 프로젝트라면 더 확인한다

아래는 판정 근거가 아니라 **어떻게 적용할지 정하는 정보**입니다.

- 지시 파일이 있다면 그 내용 — `CLAUDE.md`는 `@AGENTS.md` 한 줄인가 본문이
  들어 있는가, `.agents/`는 cairn 구조인가 전혀 다른 구조인가
- 현재 브랜치와 upstream, 원격 기본 브랜치
- 작업 트리에 커밋하지 않은 변경이 있는가
- 이 저장소가 공개인가 비공개인가

---

## 1. 브랜치 후보와 다음 번호를 확인한다

0번에서 본 현재 브랜치와 upstream, 원격 기본 브랜치, 저장소 문서와 기존 관례를
보고 어디서 시작하고 어디에 반영할 수 있는지 후보를 확인합니다. `main`이라고
가정하거나 시작점과 반영 대상이 같다고 가정하지 않습니다. 이 단계에서는 아직
작업 위치를 정하지 않습니다. 근거가 부족하거나 후보가 여럿이면 2번의 질문에
선택지를 함께 보여줍니다.

**다음 번호**는 `.agents/plans/workstreams/`와 `.agents/archive/workstreams/`
양쪽에 이미 쓰인 번호를 모두 확인해서 정합니다. 둘 다 없으면 `001`입니다. 세팅
워크스트림의 이름은 `<번호>-cairn-setup`입니다.

---

## 2. 작업 위치와 원격 작업 흔적을 묻는다

정해진 기본 작업 위치는 없습니다. 문서를 만들기 전에 아래 순서로 물어야 결정이
부트스트랩 커밋에 함께 들어갑니다.

먼저 어디서 작업할지 묻습니다.

> 이 세팅을 원하는 브랜치에서 직접 진행할까요, 아니면 별도의
> `workstream/<번호>-cairn-setup` 로컬 브랜치를 만들어 진행할까요?

직접 작업을 고르면 어느 브랜치인지 확인합니다. 그 브랜치가 기준 브랜치이자 반영
대상 브랜치입니다.

로컬 작업 브랜치를 고르면 어느 브랜치에서 갈라지고 어느 브랜치에 반영할지
확인한 다음, 원격 작업 흔적을 물어봅니다.

> 작업 브랜치를 원격에 올리고 PR 기록을 남길까요, 아니면 로컬에서만 작업한 뒤
> 대상 브랜치에 반영할까요?

흔적을 남기지 않아도 최종 커밋은 대상 브랜치에 남습니다. 남기지 않는 것은 원격
작업 브랜치, PR, 병합 커밋과 작업 브랜치 이름입니다.

---

## 3. 변경안과 완료 조건을 보여주고 승인받는다

아래를 정리해서 보여주고 승인을 받습니다. **승인 전에는 파일을 쓰지 않습니다.**

- 0번 조사 결과 — 무엇이 이미 있고 무엇이 없는지
- 새로 만들 파일과 기존 파일에서 고칠 부분
- 기존 문서에서 `.agents/`로 옮길 내용의 개요
- 골격 기본값과 어긋나 보이는 기존 규칙. 어느 쪽을 따를지는 사람이 정합니다
- 기준 브랜치와 반영 대상 브랜치, 다음 번호, 2번에서 정한 작업 위치와 원격
  작업 흔적
- 5번에서 어느 지시 파일에 어떤 연결 문구를 넣을지
- 이 세팅의 완료 조건

### `.agents/`를 커밋할지 여기서 정합니다

4번의 부트스트랩 커밋에 무엇이 들어갈지가 여기에 달려 있고, 두 선택은 결과가 다릅니다.

| | 커밋한다 | 커밋하지 않는다 |
| --- | --- | --- |
| 계획이 남는 곳 | 기준 브랜치 | 로컬 작업 공간뿐 |
| 다른 checkout·기기에서 | 확인 가능 | 확인 불가 |
| 부트스트랩 커밋에 들어가는 것 | 세팅 워크스트림과 「작업 위치」 포함 | 지시 파일 변경만. 세팅 문서는 빠짐 |
| 이어받기 | 기준 브랜치를 읽으면 됨 | 같은 작업 공간에서만 가능. 기록을 잃지 않을 별도 방법이 필요함 |

커밋하지 않기로 하면 `.gitignore`에 넣고, **계획을 커밋으로 남길 수 없다는 것을
사용자에게 알립니다.** "다른 세션이 그 커밋을 보고 이어받는다"는 전제가 이
경우에는 성립하지 않습니다.

적용 도중에 커밋할 일이 생기니 `.cairn/`도 `.gitignore`에 넣어 둡니다. 이 줄을
정리하는 방법은 세팅 워크스트림의 마지막 단계에 있습니다.

---

## 4. 세팅 워크스트림을 만든다

기준 브랜치에서 아래를 배치합니다.

1. 골격 문서를 놓습니다. 기존 파일은 덮어쓰지 않습니다.
   - 없는 것만 `.cairn/template/`에서 가져옵니다.
   - 이미 있는 `AGENTS.md`, `CLAUDE.md`, `.agents/`는 손대지 않고 그대로 둡니다.
     이관은 세팅 워크스트림에서 합니다. 예외는 아래 5번의 연결 문구뿐입니다.
   - 복사한 `AGENTS.md`가 있다면 `.agents/` 커밋 여부의 「고르기」 자리를 3번에서
     정한 결과로 바꿉니다.
2. 세팅 워크스트림 폴더를 만듭니다.

   ```bash
   mkdir -p .agents/plans/workstreams/<번호>-cairn-setup
   cp .cairn/setup-workstream/README.md .cairn/setup-workstream/workflow.md \
      .agents/plans/workstreams/<번호>-cairn-setup/
   ```

3. 복사한 두 문서의 `<!-- 채우기: -->` 자리를 0~3번에서 확인한 내용으로
   채웁니다.
4. 세팅을 어디에 적을지 루트 `.agents/plans/workflow.md`의 상태에 따라
   갈라집니다.
   - **없으면** `.cairn/template/`에서 가져와 만들고 「현재 대작업」에 적습니다.
   - **있고 cairn 형식이면** 「현재 대작업」에 항목을 더합니다.
   - **있는데 다른 형식이면** 덮어쓰지 않습니다. 어디에 어떻게 적을지 3번에서
     승인받은 최소 방법을 씁니다. 정해진 것이 없으면 사용자에게 묻습니다.

   어느 쪽이든 문서 위치, 작업 위치, 기준 브랜치와 반영 대상 브랜치를 각각
   적습니다. 로컬 작업 브랜치를 고른 경우에는 원격 작업 흔적을 남길지도 적고,
   대상 브랜치에서 직접 작업하면 `해당 없음`으로 적습니다.
5. **지금 쓰는 자동 지시 진입점 전부에서 세팅 워크스트림까지 이어지는지
   확인합니다.** 부트스트랩 커밋이 있어도 진입점이 `.agents/`를 안내하지
   않으면 새 세션은 세팅이 진행 중이라는 것을 모릅니다. 이 구조는 Codex와
   Claude Code를 함께 쓰므로 둘 다 봐야 합니다.

   - Codex가 읽는 `AGENTS.md`
   - Claude Code가 읽는 `CLAUDE.md`. `@AGENTS.md` 한 줄이라면 `AGENTS.md`만
     고치면 이어집니다. 본문이 따로 들어 있으면 여기에도 넣어야 합니다.
   - 그 밖에 0번에서 찾은 지시 파일 가운데 지금 실제로 쓰는 것

   이어지지 않는 진입점에 한 줄을 넣습니다.

   > cairn 구조 적용이 진행 중입니다. 현재 상태는
   > `.agents/plans/workstreams/<번호>-cairn-setup/workflow.md`에 있습니다.

   지시 파일을 고치는 것이므로 3번에서 어느 파일에 무슨 문구를 넣을지까지
   승인받아 둡니다. 이 줄은 세팅이 끝날 때 정식 안내표로 정리합니다.
6. 여기까지를 기준 브랜치에 커밋합니다. 이것을 **부트스트랩 커밋**이라고
   부릅니다. 작업 위치, 기준 브랜치, 반영 대상 브랜치와 원격 작업 흔적 결정이
   이 커밋에 들어가야 나중에 이어받는 세션이 같은 방향으로 작업할 수 있습니다.

   `.agents/`를 커밋하기로 했으면 계획 문서까지 이 커밋에 들어갑니다. 커밋하지
   않기로 했어도 **부트스트랩 커밋 자체는 생깁니다.** 5번의 연결 문구,
   `.gitignore`에 넣은 `.agents/`와 `.cairn/`, 새로 만든 `AGENTS.md`나
   `CLAUDE.md`는 추적되기 때문입니다. 빠지는 것은 계획 문서뿐이고, 그래서 이
   경우에는 5번의 연결 문구가 유일한 발견 경로가 됩니다.

   **커밋 자체도 승인을 받습니다.** 이 시점에는 새 `AGENTS.md`와 규칙을 아직
   다시 읽기 전이므로, 기존 프로젝트에서 지금 적용되는 커밋 승인 규칙을
   따릅니다. 별도 규칙이 없으면 스테이징된 파일과 커밋 메시지를 먼저 보여주고
   승인받은 뒤 커밋합니다. 3번의 파일 변경 승인이 커밋 승인까지 대신하지
   않습니다.
7. 작업 브랜치를 만들기로 했으면 부트스트랩 커밋에서 `workstream/<번호>-cairn-setup`을
   만듭니다. 반영 대상 브랜치에서 직접 하기로 했으면 만들지 않습니다.

---

## 5. 나머지는 세팅 워크스트림을 따른다

여기서부터는 `.agents/plans/workstreams/<번호>-cairn-setup/workflow.md`가
안내합니다. 이관, 연결, 확인, 정리, 완료와 아카이브가 거기 있습니다.

`.cairn/` 폴더를 지우는 것도 그 문서의 마지막 단계입니다. 지우기 전에 사용자가
폴더 안을 고쳤는지 실제 명령으로 확인하고, 확인이 실패하면 지우지 않습니다.

## 적용 전과 적용 후

cairn이 아직 놓이기 전에는 이 프로젝트에 워크스트림 규칙이 없습니다. 그래서
0~4번은 이 문서가 직접 안내합니다.

`.agents/plans/README.md`와 `workflow.md`가 자리를 잡고, 세팅 워크스트림이
아카이브되고, 새 `AGENTS.md`와 `.agents/rules/`를 다시 읽은 뒤부터는 그 문서들이
원본입니다. 그 시점 이후로는 이 문서를 보지 않습니다.
