# 적용 절차

이 문서는 `cairn` 골격을 프로젝트에 적용하는 절차입니다. 에이전트에게 읽히고
시켜도 되고, 사람이 직접 읽고 따라 해도 됩니다. 할 일은 같습니다.

에이전트에게 시킬 때는 이렇게 말하면 됩니다.

> `.cairn/APPLY.md`를 읽고 이 프로젝트에 적용해줘. 기존 규칙과 기록은 보존하고,
> 지시 파일 변경안은 적용 전에 보여줘.

**지시 파일은 새로 만드는 것도 승인을 받고 씁니다.** 새 프로젝트든 기존
프로젝트든, 무엇을 어떻게 쓸지 먼저 보여준 뒤에 씁니다.

---

## 0. 먼저 확인

무엇이든 쓰기 전에 아래를 확인하고 결과를 사람에게 알립니다.

**지시 파일**

- `AGENTS.md`가 이미 있는가
- `CLAUDE.md`가 이미 있는가. 있다면 `@AGENTS.md` 한 줄인가, 본문이 직접 들어
  있는가
- `.agents/`가 이미 있는가
- `.cursorrules`, `.github/copilot-instructions.md`, `.claude/rules/`처럼 다른
  에이전트 지시 파일이 있는가

**기록과 맥락** — 지시 파일이 하나도 없어도 아래는 따로 확인합니다.

- `TODO.md`, `NOTES.md`, `ROADMAP.md`, `docs/` 아래 계획 문서, 핸드오프 문서처럼
  진행 상태나 목표를 담은 문서가 있는가
- README에 목표나 범위 설명이 있는가
- 커밋 이력이 있는가

**그 밖에**

- 이 저장소가 공개인가 비공개인가

**위에서 하나라도 나오면 "기존 프로젝트"입니다.** 2번으로 갑니다. 지시 파일도
기록도 커밋도 없는 빈 저장소일 때만 1번으로 갑니다.

파일을 하나씩 쓰다가 중간에 멈추면 절반만 적용된 상태가 남습니다. 무엇을 어떻게
바꿀지 전부 정한 뒤에 쓰기 시작합니다.

적용 도중에 커밋할 일이 생길 수 있으니 `.cairn/`을 `.gitignore`에 넣어 둡니다.
폴더와 이 줄을 정리하는 절차는 아래 5번에 있습니다.

---

## 1. 새 프로젝트

### 1-1. 무엇을 쓸지 먼저 보여준다

복사할 파일 목록과, `<!-- 채우기: -->` 자리에 무엇을 넣을지 정리해서 사람에게
보여주고 승인을 받습니다. 승인 전에는 파일을 쓰지 않습니다.

### 1-2. 복사하고 채운다

```bash
cp -r .cairn/template/. .
```

`<!-- 채우기: -->` 주석이 있는 자리를 채웁니다. 어디가 남았는지는 이렇게
확인합니다.

```bash
grep -rn "채우기\|고르기" AGENTS.md .agents/
```

채울 내용은 README, `package.json`, 기존 코드에서 확인할 수 있습니다. 확인되지
않는 것은 지어내지 말고 `<!-- 확인 필요: 무엇을 -->`로 남깁니다.

`<!-- 기본값 -->`이라고 적힌 부분(답변 언어, 작업 전 승인, Git과 커밋)은 그대로
써도 되고 팀 사정에 맞게 바꿔도 됩니다.

다 채웠으면 4번으로 갑니다.

---

## 2. 기존 프로젝트

**기존 파일을 덮어쓰지 않습니다.** 골격에 있는 내용을 기존 문서에 옮겨 담고,
기존에 있던 규칙은 살립니다.

### 2-1. 무엇을 어떻게 바꿀지 먼저 정리해 보여준다

아래를 표나 목록으로 정리해서 사람에게 보여주고 승인을 받습니다. 승인 전에는
파일을 쓰지 않습니다.

- 새로 만들 파일
- 기존 파일에서 고칠 부분과 바꿀 문구
- 기존 문서에서 `.agents/`로 옮길 내용
- 골격과 기존 규칙이 서로 어긋나는 항목. 어느 쪽을 따를지는 사람이 정합니다

### 2-2. 규칙을 가른다

기존 지시 파일에 아래 성격의 내용이 섞여 있으면 해당 파일로 옮깁니다.

- 완료 전 확인 방법, 테스트·빌드 명령 → `.agents/rules/verification.md`
- 답변 방식, 설명 태도 → `.agents/rules/communication.md`
- 목적, 배경, 장기 방향, 범위의 상세 설명 → `.agents/plans/goal.md`

**항상 지켜야 하는 제약은 `AGENTS.md`에 남깁니다.** 옮기는 것은 상세 설명이지
제약 자체가 아닙니다. 특히 「하지 않는 것」은 작업 중에 매번 걸리는 선이라
`AGENTS.md`에 짧게 남기고, 그렇게 정한 배경만 `goal.md`로 옮깁니다.

무엇이 남고 무엇이 옮겨 가는지는 이렇게 가릅니다.

| 성격 | 어디에 |
| --- | --- |
| 작업할 때마다 걸리는 제약, 금지 사항, 승인 규칙 | `AGENTS.md`에 짧게 |
| 그 제약을 왜 정했는지, 배경과 장기 방향 | `.agents/plans/goal.md` |
| 특정 작업을 할 때만 필요한 절차와 체크리스트 | 해당 문서에 두고 `AGENTS.md` 안내표에서 가리킴 |

### 2-3. 기록을 옮긴다

기존에 진행 기록을 담은 문서(`TODO.md`, `NOTES.md`, 핸드오프 문서 등)가 있으면
성격에 따라 나눠 옮깁니다. 루트 `workflow.md`는 시작점이고 `history.md`는 짧은
색인이라, 상세 내용을 여기에 쌓지 않습니다.

| 무엇 | 어디로 |
| --- | --- |
| 진행 중인 작업의 상세 기록 | `.agents/plans/workstreams/<번호>-<이름>/workflow.md` |
| 그 작업의 배경·범위·완료 조건 | 같은 폴더의 `README.md` |
| 진행 중인 작업이 어디 있는지 가리키는 줄 | 루트 `.agents/plans/workflow.md` |
| 이미 끝난 작업의 상세 기록 | `.agents/archive/workstreams/<번호>-<이름>/` |
| 끝난 작업의 한 줄 요약과 보관 위치 | `.agents/plans/history.md` |
| 아직 착수를 약속하지 않은 것 | `.agents/plans/ideas.md` |

원본 문서를 지울지는 사람에게 묻습니다.

### 2-4. 연결한다

**이 단계를 빠뜨리면 파일만 생기고 아무도 읽지 않습니다.**

- `AGENTS.md`에 문서 안내표를 넣습니다. 기존 안내가 있으면 항목을 더합니다.
- `CLAUDE.md`가 없으면 `@AGENTS.md` 한 줄로 만듭니다. 본문이 직접 들어 있으면
  그 내용을 `AGENTS.md`로 옮기고 한 줄로 줄일지 사람에게 묻습니다.
- `.agents/` 커밋 여부를 정하고, 커밋하지 않기로 했으면 `.gitignore`에
  넣습니다.

### 2-5. 현재 상태를 채운다

`goal.md`와 `workflow.md`를 빈 틀로 두지 않습니다. README, 코드, `git log`,
열린 이슈에서 확인되는 것으로 채웁니다. 확인되지 않는 것은
`<!-- 확인 필요: 무엇을 -->`로 남기고 사람에게 무엇이 비었는지 알립니다.

---

## 3. 어긋나는 규칙을 만났을 때

기존 규칙과 골격이 충돌하면 조용히 덮어쓰지 않습니다. 무엇과 무엇이 어긋나는지
짚고 어느 쪽을 따를지 사람에게 묻습니다. 흔한 경우는 이렇습니다.

- 커밋 메시지 언어와 형식 — 기존 `git log`를 따르는 것이 보통 맞습니다
- 승인 범위 — 팀마다 다릅니다
- 이미 다른 문서 체계를 쓰고 있는 경우 — 섞을지 한쪽으로 통일할지 정해야 합니다

---

## 4. 적용됐는지 확인

파일이 생긴 것만으로는 확인이 되지 않습니다. 아래까지 봐야 적용된 것입니다.

**문서로 확인**

- `grep -rn "채우기\|고르기\|확인 필요" AGENTS.md .agents/` 결과에 남은 것이
  무엇인지 사람에게 알렸는가
- `CLAUDE.md`에서 `AGENTS.md`로, `AGENTS.md`의 안내표에서 `.agents/`의 각
  파일로 실제로 이어지는가. 안내표에 적힌 경로가 실재하는가
- 기존에 있던 규칙 중 사라진 것이 없는가. 옮겼다면 옮긴 자리에 실제로 있는가
- 같은 규칙이 두 곳에 원본으로 중복되어 있지 않은가
- `grep -rn "\.cairn" AGENTS.md CLAUDE.md .agents/` 결과가 비어 있는가.
  적용한 문서가 곧 지울 폴더를 가리키면 안 됩니다

**실제로 확인**

- 새 세션에서 "지금 뭐 하던 중이었지?"라고 물었을 때 에이전트가
  `.agents/plans/workflow.md`를 찾아 읽고 답하는가

## 5. 정리

지우기 전에 확인합니다.

- 위 4번의 `.cairn` 참조 검사가 통과했는가
- `.cairn/` 안의 파일을 고친 것이 없는가. 고쳤다면 그 내용이 프로젝트 쪽에
  반영됐는지 먼저 확인합니다

```bash
git -C .cairn status --short
```

**이 명령이 승인 거부나 오류로 실패하면 확인되지 않은 것입니다.** 내가 이
폴더를 편집하지 않았다는 것은 사용자가 편집하지 않았다는 증거가 아닙니다.
확인될 때까지 지우지 말고, 무엇을 확인하지 못했는지 사람에게 알립니다.

확인이 끝나면 폴더를 지웁니다.

```bash
rm -rf .cairn
```

`.gitignore`에서는 **이번 적용에서 추가한 `.cairn/` 줄만** 지웁니다. 원래 있던
항목은 건드리지 않습니다.
